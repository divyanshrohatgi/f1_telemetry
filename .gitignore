# Python
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.pyc
backend/.env
frontend/.env
.env
.env.*

# FastF1 cache (large binary files — regenerated on first run)
backend/cache/

# Data files — raw parquets and outputs (rebuild with ml/train.py)
backend/data/

# Virtual environments
backend/.venv/
.venv/

# ML models (binary, too large for git — rebuild with train.py)
backend/ml/saved_models/*.pkl

# Node
frontend/node_modules/
frontend/dist/

# IDE / tools
.vscode/
.idea/
*.swp
.claude/

# OS
.DS_Store
Thumbs.db
